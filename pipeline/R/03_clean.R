# Nettoyage, réconciliation des identités, jointure performances/contrats. Échoue au-delà de 2 % de non-résolution.
#
# Entrées  : raw/*
# Sorties  : interim/player_games.parquet, interim/unmatched.csv
# Phase    : 1 du brief.

whisker_step_03_clean <- function(paths = whisker_paths()) {
  whisker_not_implemented("03_clean", "1")
}
