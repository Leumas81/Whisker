# Interrogation de l'API Cargo de Leaguepedia : joueurs, tenures, contrats. Pagination par offset, en-tête User-Agent identifiant le projet, une seconde de pause entre requêtes, réponses mises en cache.
#
# Entrées  : config/leagues.yaml
# Sorties  : raw/leaguepedia_*.json
# Phase    : 1 du brief.

whisker_step_02_contracts <- function(paths = whisker_paths()) {
  whisker_not_implemented("02_contracts", "1")
}
