# Écriture des cinq JSON, validation contre les schémas, arrondis, meta.json avec le hash git.
#
# Entrées  : interim/*.rds
# Sorties  : web/src/data/*.json
# Phase    : 3 du brief.

whisker_step_08_export <- function(paths = whisker_paths()) {
  whisker_not_implemented("08_export", "3")
}
