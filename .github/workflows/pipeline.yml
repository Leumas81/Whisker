name: pipeline

# Recalcul des estimations à partir des sources réelles.
#
# Le pipeline tourne ici et nulle part ailleurs : le propriétaire n'a rien à exécuter. Deux
# raisons de le lancer depuis la CI plutôt que d'une machine personnelle :
#
#   — l'API Cargo de Leaguepedia limite fortement le débit des adresses mutualisées, ce qui
#     rend un tirage de quarante mille lignes impraticable depuis un accès domestique ;
#   — le résultat doit être reproductible et attribuable à un commit, pas à un poste.

on:
  schedule:
    # Lundi 04h00 UTC : les splits se jouent le week-end, les données sont stabilisées.
    - cron: "0 4 * * 1"
  workflow_dispatch:
    inputs:
      steps:
        description: "Étapes à rejouer (vide = toutes)"
        required: false
        default: ""
      replicates:
        description: "Réplications de bootstrap (2000 en régime normal)"
        required: false
        default: "2000"
      reprise:
        description: "Numéro de reprise, renseigné automatiquement"
        required: false
        default: "0"
  push:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: pipeline
  cancel-in-progress: false

jobs:
  # Sur simple poussée, le pipeline ne tourne que si les données commitées sont encore
  # synthétiques. La condition s'éteint d'elle-même dès le premier passage réussi : le premier
  # push amorce le remplacement, les suivants ne relancent rien.
  decider:
    runs-on: ubuntu-latest
    outputs:
      lancer: ${{ steps.verdict.outputs.lancer }}
    steps:
      - uses: actions/checkout@v4
      - id: verdict
        run: |
          if [ "${{ github.event_name }}" != "push" ]; then
            echo "lancer=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          if [ ! -f web/src/data/meta.json ]; then
            echo "Aucune donnée commitée : le pipeline doit tourner."
            echo "lancer=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          synthetique=$(node -e "console.log(require('./web/src/data/meta.json').synthetic)")
          echo "meta.synthetic = $synthetique"
          echo "lancer=$synthetique" >> "$GITHUB_OUTPUT"

  recalculer:
    needs: decider
    if: needs.decider.outputs.lancer == 'true'
    runs-on: ubuntu-latest
    # Le tirage a son propre budget (WHISKER_FETCH_BUDGET_MINUTES) et s'arrête en le disant.
    # Ce plafond-ci ne couvre plus qu'un imprévu : une exécution qui s'y heurte est un défaut
    # à corriger, pas une durée normale. La première version, à 330 minutes, a laissé un
    # tirage bloqué brûler cinq heures et demie sans rien produire.
    timeout-minutes: 120

    env:
      WHISKER_REPLICATES: ${{ github.event.inputs.replicates || '2000' }}
      WHISKER_FETCH_BUDGET_MINUTES: "45"

    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: "4.4.2"
          use-public-rspm: true

      # V8, dont dépend jsonvalidate, ne s'installe pas sans cette bibliothèque système.
      # Sans elle, c'est toute la validation du contrat de données qui tombe.
      - name: Dépendances système
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libnode-dev libcurl4-openssl-dev libssl-dev libxml2-dev

      - name: Cache de la bibliothèque R
        uses: actions/cache@v4
        with:
          path: ~/.cache/R/renv
          key: renv-${{ runner.os }}-${{ hashFiles('pipeline/renv.lock') }}
          restore-keys: renv-${{ runner.os }}-

      # Voir .github/workflows/_r-setup.md pour le détail. En deux mots : le lockfile déclare
      # un dépôt de sources, ce qui ferait compiler arrow et lme4 sur le runner. On force
      # renv à utiliser celui que setup-r a configuré, qui sert des binaires.
      - name: Restaurer l'environnement R
        working-directory: pipeline
        run: |
          # Le dépôt binaire se déduit de la distribution du runner, sans passer par R : lire
          # la configuration de setup-r depuis R dépendait du nom qu'il donne à son dépôt, et
          # une clé absente faisait échouer l'étape en une seconde.
          CODENAME=$(. /etc/os-release && echo "$VERSION_CODENAME")
          REPO="https://packagemanager.posit.co/cran/__linux__/${CODENAME}/latest"
          echo "distribution : $CODENAME"
          echo "dépôt        : $REPO"

          # renv est installé explicitement avant que le profil du projet ne tente de le faire
          # depuis le dépôt du lockfile, qui ne sert que des sources.
          R_PROFILE_USER=/dev/null Rscript -e "install.packages('renv', repos = '$REPO')"

          export RENV_CONFIG_REPOS_OVERRIDE="$REPO"
          Rscript -e 'renv::restore(prompt = FALSE)'
          Rscript -e 'cat("paquets restaurés :", nrow(installed.packages()), "\n")'

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Installer les dépendances Node
        run: pnpm install --frozen-lockfile

      # Le cache des réponses porte la reprise : la source ne laisse passer qu'une requête
      # sur quatre, et un tirage complet demande plusieurs exécutions. Chaque page obtenue
      # est conservée, donc rien n'est retéléchargé d'une exécution à l'autre.
      - name: Cache des réponses de source
        uses: actions/cache@v4
        with:
          path: pipeline/raw
          key: sources-leaguepedia-${{ github.run_id }}
          restore-keys: |
            sources-leaguepedia-
            sources-

      - name: Exécuter le pipeline
        id: pipeline
        continue-on-error: true
        working-directory: pipeline
        run: Rscript run_all.R ${{ github.event.inputs.steps }}

      # Un tirage incomplet n'est pas un défaut du code : la source refuse une partie des
      # requêtes, et ce qui a été obtenu est en cache. On relance donc, en bornant le nombre
      # de reprises pour qu'une source durablement fermée ne tourne pas en boucle.
      - name: Reprendre le tirage si la source en a refusé une partie
        if: steps.pipeline.outcome == 'failure' && hashFiles('pipeline/interim/tirage_incomplet') != ''
        env:
          GH_TOKEN: ${{ github.token }}
          REPRISE: ${{ github.event.inputs.reprise || '0' }}
        run: |
          manquantes=$(cat pipeline/interim/tirage_incomplet)
          echo "### Tirage incomplet" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "$manquantes page(s) refusée(s) par la source. Reprise n° $REPRISE." >> "$GITHUB_STEP_SUMMARY"

          if [ "$REPRISE" -ge 6 ]; then
            echo "Six reprises sans aboutir : la source est durablement fermée." >> "$GITHUB_STEP_SUMMARY"
            exit 1
          fi

          gh workflow run pipeline.yml             --field steps=""             --field replicates="${{ env.WHISKER_REPLICATES }}"             --field reprise="$(( REPRISE + 1 ))"
          echo "Reprise déclenchée." >> "$GITHUB_STEP_SUMMARY"
          exit 1

      - name: Signaler un échec qui n'est pas une reprise
        if: steps.pipeline.outcome == 'failure' && hashFiles('pipeline/interim/tirage_incomplet') == ''
        run: exit 1

      - name: Valider les cinq JSON contre les schémas
        run: node tools/validate-data.mjs --require-all

      - name: Vérifier que les types suivent les schémas
        run: node tools/gen-types.mjs --check

      - name: Construire le site avec les nouvelles données
        run: pnpm --filter whisker-web run build

      - name: Résumer ce qui a été produit
        run: |
          node -e "
            const meta = require('./web/src/data/meta.json');
            const { players } = require('./web/src/data/players.json');
            console.log('source          :', meta.performanceSource);
            console.log('synthétique     :', meta.synthetic);
            console.log('composants      :', meta.metricComponents.join(', '));
            console.log('joueurs         :', players.length);
            console.log('lignes          :', meta.counts.playerGames);
            console.log('dernière donnée :', meta.lastDataDate);
            console.log('premiers noms   :', players.slice(0, 8).map(p => p.name).join(', '));
          "

      - name: Conserver les diagnostics des modèles
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: diagnostics
          path: pipeline/diagnostics/
          retention-days: 30

      - name: Commiter les données si elles ont changé
        run: |
          git config user.name "whisker-pipeline"
          git config user.email "pipeline@whisker.invalid"
          git add web/src/data
          if git diff --cached --quiet; then
            echo "Aucun changement de données : rien à commiter."
          else
            git commit -m "data(pipeline): recalcul des estimations sur les sources réelles"
            git push
          fi
